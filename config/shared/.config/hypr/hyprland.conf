# Hyprland Lua config is the canonical entrypoint for this setup.
# Hyprland 0.55+ loads ~/.config/hypr/hyprland.lua when available.
# Legacy split .conf fallback files were removed; keep changes in lua/*.lua.
