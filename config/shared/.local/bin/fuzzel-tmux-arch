#!/bin/sh
set -eu

selection=$(distrobox-enter arch -- tmux ls 2>/dev/null | hypr-dmenu --prompt "tmux> ")
[ -n "${selection:-}" ] || exit 0
session=${selection%%:*}
exec kitty -e distrobox-enter arch -- tmux attach -t "$session"
