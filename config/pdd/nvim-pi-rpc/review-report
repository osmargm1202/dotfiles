# Review Report — nvim-pi-rpc

## group: review-alpha
**Status:** CONDITIONAL
**Findings:**
- [CRITICAL] `nvim/.config/nvim/lua/pi_rpc/client.lua:178-207` collects streamed `text_delta` chunks but does not handle RPC `assistantMessageEvent.type == "error"`. Pi RPC docs define `error` as valid streaming event. Current client can collapse provider/runtime failures into generic `"PiAsk: no assistant text returned"` or exit-code fallback, so transport layer loses protocol error semantics.
- [WARNING] `nvim/.config/nvim/lua/pi_rpc/client.lua:213-227,271-291` does not normalize CRLF-framed JSONL records. Pi RPC framing docs say clients should strip trailing `\r` before JSON decode. Current parser splits on `\n` only, then decodes raw line; `\r\n` records would parse as malformed JSON.
- [WARNING] Required review inputs missing: `pdd/nvim-pi-rpc/requirements`, `pdd/nvim-pi-rpc/plan`, `pdd/nvim-pi-rpc/build-progress` not present in workspace, so plan-step validation for `review-alpha` could not be completed against source-of-truth docs.

## Consolidated verdict
- Overall status: CONDITIONAL
- Recommended next step: fix transport protocol gaps above, restore PDD docs in workspace, rerun review-alpha against actual plan steps.
