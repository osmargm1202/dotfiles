{
  "path": "sdd-orchestrator/pi-todo/tdd/build-progress",
  "group": "Task 1: Add todo core and tool",
  "status": "completed",
  "files_changed": [
    "config/shared/.pi/agent/extensions/todo.ts",
    "sdd-orchestrator/pi-todo/tdd/build-progress"
  ],
  "tdd_applicability_reason": "Applicable: captured initial missing-extension failure before implementation, then verified extension load and todo tool behavior after implementation.",
  "verification": [
    "RED: pi -p --no-extensions --extension ./config/shared/.pi/agent/extensions/todo.ts \"respond ok\" --no-builtin-tools failed because todo.ts did not exist.",
    "GREEN: pi -p --no-extensions --extension ./config/shared/.pi/agent/extensions/todo.ts \"respond ok\" --no-builtin-tools returned ok.",
    "POST-COMMIT: pi -p --no-extensions --extension ./config/shared/.pi/agent/extensions/todo.ts \"respond ok\" --no-builtin-tools returned ok.",
    "POST-COMMIT: pi -p --no-extensions --extension ./config/shared/.pi/agent/extensions/todo.ts \"Use the todo tool to create a task with subject Verify core.\" --no-builtin-tools returned: Created todo #1: Verify core.",
    "Attempted direct helper script via node+jiti; blocked because jiti is not installed in the worktree environment."
  ],
  "risks": [
    "Task 1 intentionally does not implement replay, /todos command, or overlay; those remain for Task 2.",
    "No project test harness exists for this standalone extension, so verification is Pi sanity-load/tool-call based."
  ]
}
