# Build Progress: tmux micro-cleanup (dir selection, focus pane, tmuxfd)

## Completed

- Added `__tmux_dir_history_select` helper to `__tmux_shared.fish` — extracts dir.txt reading, dedup, and fzf selection from tmuxnew
- Added `TMUX_FOCUS_PANE` config variable (default 1) to `__tmux_shared.fish` — user can override with `set -g TMUX_FOCUS_PANE N`
- Updated `__tmux_create_session` to use `$TMUX_FOCUS_PANE` instead of hardcoded `0.1`
- Refactored `tmuxnew.fish` to call `__tmux_dir_history_select` (removed ~15 lines of inline dir.txt logic)
- Refactored `tmuxfd.fish` to use shared helpers directly (`__tmux_init`, `__tmux_find_session`, `__tmux_session_name`, `__tmux_create_session`, `__tmux_dir_history_append`) instead of delegating to `tmuxnew`
- `tmuxnewz.fish` unchanged (already clean)
- `config.fish` unchanged (already sources `__tmux_shared.fish`)
- All 4 changed files pass `fish --no-execute` syntax validation

## Files Changed

- `config/fish/functions/__tmux_shared.fish` — added `__tmux_dir_history_select`, `TMUX_FOCUS_PANE` var, updated `__tmux_create_session`
- `config/fish/functions/tmuxnew.fish` — uses `__tmux_dir_history_select` instead of inline dir.txt logic
- `config/fish/functions/tmuxfd.fish` — uses shared helpers directly, no longer wraps tmuxnew
- `config/fish/functions/tmuxnewz.fish` — unchanged

## Deviations

None. Behavior preserved exactly.

## Blockers/Risks

None.

## Remaining

None.
